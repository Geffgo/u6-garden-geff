---
{"dg-publish":true,"permalink":"/1-2-vm-serveur-glpi/","dg-note-properties":{}}
---

# Fiche Technique : VM Serveur GLPI (VirtualBox)

> [!ATTENTION]
> La VM abrite plusieurs services vérifier que tout les autres services sont désactiver:
> - HaProxy
> - DHCP
> - DNS (si pas besoin)

### 1. Informations de la VM
- **Nom de la VM** : `VM-SERVEUR`
- **OS** : Linux Debian 12 (64-bit)
- **Accès Système** :
    - **Utilisateur** : `sysadmin`        
    - **Mot de passe** : `netlab123`
- **Accès Web GLPI (par défaut)** : `glpi` / `glpi`

### 2. Installation et Dépendances

Connectez-vous en `root` ou utilisez `sudo`.

**Mise à jour et paquets nécessaires (Apache, MariaDB, PHP 8.2) :**

Bash

```
apt update && apt upgrade -y
apt install -y apache2 mariadb-server php php-curl php-gd php-imagick php-intl php-apcu php-memcache php-mysqli php-sqlite3 php-zip php-bz2 php-ldap php-xml php-mbstring php-bcmath php-exif
```

### 3. Base de Données (MariaDB)

1. **Sécurisation** : `mysql_secure_installation`
2. **Configuration SQL** :

SQL

```
CREATE DATABASE glpi CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'glpi_user'@'localhost' IDENTIFIED BY 'netlab123';
GRANT ALL PRIVILEGES ON glpi.* TO 'glpi_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

### 4. Déploiement de GLPI

**Téléchargement et extraction :**

Bash

```
cd /tmp
wget https://github.com/glpi-project/glpi/releases/download/10.0.24/glpi-10.0.24.tgz
tar -xvzf /tmp/glpi-10.0.24.tgz -C /var/www/
```

**Gestion des permissions :**

Bash

```
chown -R www-data:www-data /var/www/glpi
chmod -R 755 /var/www/glpi
```

---

### 5. Configuration Apache2

1. **Créer le VirtualHost** : `vi /etc/apache2/sites-available/glpi.conf`
2. **Contenu du fichier** :

Apache

```
<VirtualHost *:80>
    DocumentRoot /var/www/glpi/public
    <Directory /var/www/glpi/public>
        Require all granted
        RewriteEngine On
        RewriteCond %{HTTP:Authorization} ^(.+)$
        RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteRule ^(.*)$ index.php [QSA,L]
    </Directory>
</VirtualHost>
```

3. **Activation du site** :
Bash

```
a2ensite glpi.conf
a2dissite 000-default.conf
a2enmod rewrite
systemctl restart apache2
```

---

### 6. Finalisation (Interface Web)

Lien : `http://<IP_DE_VOTRE_SERVEUR>`

| **Paramètre**        | **Valeur**  |
| -------------------- | ----------- |
| **Serveur SQL**      | `localhost` |
| **Utilisateur SQL**  | `glpi_user` |
| **Mot de passe SQL** | `netlab123` |
| **Base de données**  | `glpi`      |
> [!COMPTE]
> 
> - **Admin** : `glpi` / `glpi`
> - **Technicien** : `tech` / `tech`
> -  **Utilisateur** : `normal` / `normal`


### 7. Aide-mémoire des commandes utiles

|Élément|Commande|Résultat attendu|
|---|---|---|
|Apache|`systemctl status apache2`|`active (running)`|
|HTTP|`curl http://localhost`|Code HTML (`<html>`)|
|SSH|`systemctl status sshd`|`active (running)`|
|Outils réseau|`which curl nc ss nmap`|Chemins affichés|
|Docker|`docker --version`|Version affichée|
|Image Technitium|`docker images \| grep -i technitium`|Image présente|
